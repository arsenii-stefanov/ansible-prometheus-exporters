{
  "ID": "{{ inventory_hostname }}_blackbox_exporter",
  "Name": "{{ inventory_hostname }}_blackbox_exporter",
  "Tags": [
    "prometheus-exporters",
    "blackbox_exporter",
    "{{ inventory_hostname }}",
    "hostname-{{ inventory_hostname }}",
    "icmp_ping-true"
  ],
  "Address": "{{ ansible_host }}"
}
